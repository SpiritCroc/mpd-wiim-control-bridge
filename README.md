# mpd-wiim-bridge

Expose a WiiM player through a small MPD-compatible TCP server.

This is intended for MPD clients that only need current playback metadata and basic controls. The bridge polls the WiiM HTTP API and translates MPD commands to WiiM `setPlayerCmd:*` requests.

AI disclaimer:  
This codebase is almost completely generated by GPT-5.5 (using opencode),
from pointing it both at [the WiiM API documentation](https://www.wiimhome.com/pdf/HTTP%20API%20for%20WiiM%20Products.pdf),
and my [mdp-mpris-bridge-rs](https://github.com/SpiritCroc/mpd-mpris-bridge-rs) codebase as similar reference implementation,
which itself the other hand was 100% written by hand (and can thus guide AI to yield better results).
Also I did skim the code since I don't trust LLMs blindly.

## Usage

```sh
cargo run -- --wiim-host 192.168.1.42
```

Defaults:

- MPD listen address: `0.0.0.0`
- MPD listen port: `6602`
- WiiM scheme: `https`
- WiiM TLS certificate validation: disabled
- WiiM poll interval: `1000` ms

Useful options:

```sh
cargo run -- --wiim-host 192.168.1.42 --port 6602 --scheme http --poll-ms 500
```

`--wiim-host` may also include a full URL, for example `https://192.168.1.42`.

## Supported MPD Commands

- Playback: `play`, `pause`, `stop`, `next`, `previous`, `seek`, `seekcur`
- Metadata/status: `currentsong`, `status`, `idle`, `noidle`
- Volume: `getvol`, `setvol`, `volume`
- Playback mode: `repeat`, `random`, `single`
- Outputs: `outputs`, `enableoutput`, `disableoutput`, `toggleoutput`
- Compatibility/static: `commands`, `tagtypes`, `ping`, `close`, `playlistinfo`, `lsinfo`, `stats`, command lists

## WiiM Mapping

- `getPlayerStatus` provides playback state, elapsed time, duration, volume, mute, playlist index, and loop mode.
- `getMetaInfo` provides title, artist, album, and album art URL.
- `setPlayerCmd:resume`, `pause`, `stop`, `next`, `prev`, `seek:<seconds>`, and `vol:<0-100>` are used for controls.
- MPD `repeat` and `random` are mapped to WiiM loop modes when the combination is exact. Unsupported MPD combinations, such as random without repeat, are accepted as compatibility no-ops.
- MPD `single` is maintained as compatibility state. `single oneshot` pauses after the observed track changes, matching the behavior of the MPRIS bridge. WiiM single-track loop mode is not used because it does not match MPD `single` semantics.
- `getNewAudioOutputHardwareMode` and `setAudioOutputHardwareMode:<id>` expose WiiM hardware output modes as virtual MPD outputs: `1` SPDIF, `2` AUX, `3` COAX. `disableoutput` is accepted as a no-op because WiiM does not expose a disabled hardware output state.

Adding URLs to play is intentionally not implemented.
